{
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "typescriptPath": {
      "description": "Path to the `typescript` instance that will be used for checking.",
      "type": "string"
    },
    "configFile": {
      "description": "Path to the `tsconfig.json` that describes the program. Searched for upwards from `context` when unset.",
      "type": "string"
    },
    "compilerOptions": {
      "description": "Compiler options to override the ones the config file sets.",
      "type": "object"
    },
    "ignoreDiagnostics": {
      "description": "The codes of the diagnostics not to report, as `tsc` prints them after `TS`.",
      "type": "array",
      "items": {
        "type": "number"
      }
    },
    "build": {
      "description": "Build the projects the config file references first, the way `tsc -b` does, and report what the whole solution finds.",
      "type": "boolean"
    }
  }
}
