---
"diagnostics-webpack-plugin": minor
---

Add `ignoreDiagnostics` to the `typescript` check, the codes of the diagnostics not to report — a linter's rule can be turned off in its own configuration, a TypeScript diagnostic cannot.
